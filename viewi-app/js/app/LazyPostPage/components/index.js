import { LazyPostPage, LazyPostPage_x, LazyPostPage_t } from "./LazyPostPage";
import "../../../modules/LazyPostPage";

export const components = {
    LazyPostPage_x,
    LazyPostPage_t,
    LazyPostPage,
};

window.ViewiApp.demo.publish("LazyPostPage", components);
