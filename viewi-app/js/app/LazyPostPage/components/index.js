import { LazyPostPage, LazyPostPage_x, LazyPostPage_t } from "./LazyPostPage";

export const components = {
    LazyPostPage_x,
    LazyPostPage_t,
    LazyPostPage,
};
window.ViewiApp.default.publish("LazyPostPage", components);
